import { RouteLoadingPanel } from "@/components/common/route-fallbacks";

export default function Loading() {
  return <RouteLoadingPanel copy="Preparing dashboard aggregates and manager intelligence panels." title="Loading Dashboard" />;
}
